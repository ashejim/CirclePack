# Python packages for running demo_circlepack.ipynb with pip.
# (The converters and the magic themselves need only the standard library +
# IPython, which ships with jupyterlab. uv users can skip this and run:
#   uv run --python 3.12 --with jupyterlab --with ipykernel python -m jupyterlab )
jupyterlab
ipykernel
