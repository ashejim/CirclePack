# Minimum Python dependencies for building the CirclePack user guide
# with Jupyter Book 2 (the MyST Document Engine-based version).
#
# Install with:  pipenv install -r requirements-jb.txt
# Or with pip:   pip install -r requirements-jb.txt
#
# Jupyter Book 2 is a thin Python wrapper around the Node.js-based
# MyST Document Engine. The `jupyter-book` package uses `nodeenv` to
# manage an isolated Node.js runtime automatically, so you do NOT need
# to install Node.js separately.

jupyter-book>=2.1.0          # The Jupyter Book 2 CLI (MyST-based)
mystmd>=1.3.0                # MyST Markdown CLI (pulled in by jupyter-book, pinned for safety)
nodeenv>=1.9.0               # Bootstraps a local Node.js runtime for the MyST engine

# --- Optional but commonly useful for MyST user guides ---
# Uncomment any you need:
#
# jupyter>=1.1.0             # If you want to author or execute .ipynb notebooks locally
# ipykernel>=6.29.0          # Required kernel if you execute notebooks during build
# matplotlib>=3.8.0          # If the CirclePack guide embeds plots generated in notebooks
# numpy>=1.26.0              # Common scientific-python dependency
