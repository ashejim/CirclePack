<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0">

    <id>circlepack</id>
    <formats><format>jar</format></formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <!-- Only the runCirclePack launcher classes -->
        <fileSet>
            <directory>${project.build.outputDirectory}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>runCirclePack/**</include>
            </includes>
        </fileSet>

        <!-- Bundle the cpcore.jar built earlier in the same mvn package run -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>cpcore.jar</include>
            </includes>
        </fileSet>
    </fileSets>

</assembly>
